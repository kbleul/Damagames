---
name: 'Deploy Docker Containers'

on:
  workflow_call:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Set version
      run: |
        echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> frontend/.env.example
        echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV

    - name: Install SSH key
      uses: shimataro/ssh-key-action@v2
      with:
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        #name: id_rsa # optional
        known_hosts: ${{ vars.SSH_FINGERPRINT }}
        #config: ${{ secrets.CONFIG }} # ssh_config; optional
        #if_key_exists: fail # replace / ignore / fail; optional (defaults to fail)

    - name: Create docker context
      run: docker context create test --docker "host=ssh://${{ vars.REMOTE_USER }}@${{ vars.REMOTE_HOST }}"

    # - name: Remove old php container to force build
    #   run: docker-compose --context prod -f docker-compose.prod.yml rm -s -f

    # - name: Stop containers
    #   run: docker-compose --context prod -f docker-compose.prod.yml down --remove-orphans

    - name: Deploy Docker Containers
      run: |
        docker-compose --context test -f docker-compose.stage.yml up --build --remove-orphans -d
      env:
        DOCKER_HOST: ssh://${{ vars.REMOTE_USER }}@${{ vars.REMOTE_HOST }}
        REMOTE_HOST: ${{ vars.REMOTE_HOST }}
        #DOCKER_TLS_VERIFY: 1
        #DOCKER_CERT_PATH: /github/workspace/.ssh
        APP_ENV: ${{ vars.APP_ENV }}
        DB_CONNECTION: ${{ vars.DB_CONNECTION }}
        DB_HOST: ${{ vars.DB_HOST }}
        DB_PORT: ${{ vars.DB_PORT }}
        DB_DATABASE: ${{ vars.DB_DATABASE }}
        DB_USERNAME: ${{ vars.DB_USERNAME }}
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        RELEASE_VERSION: ${{ env.RELEASE_VERSION }}
        TELEBIRR_APP_ID: ${{ env.TELEBIRR_APP_ID }}
        TELEBIRR_APP_KEY: ${{ env.TELEBIRR_APP_KEY }}
        TELEBIRR_PUBLIC_KEY: ${{ env.TELEBIRR_PUBLIC_KEY }}
        TELEBIRR_SHORT_CODE: ${{ env.TELEBIRR_SHORT_CODE }}
        TELEBIRR_TB_APP_URL: ${{ env.TELEBIRR_TB_APP_URL }}
        TELEBIRR_TB_WEB_URL: ${{ env.TELEBIRR_TB_WEB_URL }}
        TELEBIRR_TB_SDK_URL: ${{ env.TELEBIRR_TB_SDK_URL }}
        TELEBIRR_NOTIFY_URL: ${{ env.TELEBIRR_NOTIFY_URL }}
        TELEBIRR_RETURN_URL: ${{ env.TELEBIRR_RETURN_URL }}
        TELEBIRR_SUBJECT: ${{ env.TELEBIRR_SUBJECT }}
        TELEBIRR_RECIEVE_NAME: ${{ env.TELEBIRR_RECIEVE_NAME }}
        OTP_URL: ${{ env.OTP_URL }}
        OTP_USERNAME: ${{ env.OTP_USERNAME }}
        OTP_PASSWORD: ${{ env.OTP_PASSWORD }}
