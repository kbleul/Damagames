# how to run

```
export APP_ENV=dev
export DB_DATABASE=dama
export DB_USERNAME=dama
export DB_PASSWORD=
export REDIS_PASSWORD=
export PUSHER_APP_ID=1515042
export PUSHER_APP_KEY=
export PUSHER_APP_SECRET=
export PUSHER_HOST=
export PUSHER_PORT=443
export PUSHER_SCHEME=https
export PUSHER_APP_CLUSTER=ap2

docker-compose up
```